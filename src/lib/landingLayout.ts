/**
 * Horizontal layout for marketing pages — must match the inner wrapper in `LandingHeader`
 * so body content aligns with logo and nav edges.
 */
export const LANDING_PAGE_CONTAINER_CLASS =
  "max-w-7xl mx-auto w-full min-w-0 px-4 xs:px-5 sm:px-6 lg:px-8";
